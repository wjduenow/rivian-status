# PlatformIO
.pio/
.vscode/

# Claude Code scratch worktrees (`git worktree add .claude/worktrees/<name>`)
.claude/worktrees/

# Secrets — WiFi + Rivian credentials live here, never committed (plan §1).
include/secrets.h

# hardware/ — Python CAD toolchain byproducts (STLs + previews ARE committed)
__pycache__/
*.pyc
